describe('Initial test', () => {
    it('test should be true', () => {
        const result = true;
        expect(result).toBe(true)
    })
})

describe('Testando a rota Adicionar nova tarefa', () => {
    it('É possível adicionar uma nova tarefa com sucesso', () => {

    })
})